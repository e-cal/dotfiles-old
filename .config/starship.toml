format = """
$battery\
$time\
$hg_branch\
$docker_context\
$package\
$cmake\
$dart\
$dotnet\
$elixir\
$elm\
$erlang\
$golang\
$helm\
$java\
$julia\
$nim\
$nodejs\
$ocaml\
$perl\
$php\
$purescript\
$python\
$ruby\
$rust\
$swift\
$terraform\
$zig\
$nix_shell\
$conda\
$memory_usage\
$aws\
$gcloud\
$openstack\
$env_var\
$crystal\
$custom\
$cmd_duration\
$lua\
$jobs\
$git_branch\
$git_commit\
$git_state\
$git_status\

$line_break\
$username\
$hostname\
$shlvl\
$kubernetes\
$directory\
$status\
$character\
"""


[character]
success_symbol = "[](bold green)"
vicmd_symbol = "[](bold yellow)"
error_symbol = "[](bold red)"

[battery]
full_symbol = ""
charging_symbol = "⚡"
discharging_symbol = "  "


[[battery.display]]
threshold = 25
style = "bold red"

[[battery.display]]
threshold = 50
style = "yellow"

[[battery.display]]
threshold = 100
style = "green"

[cmd_duration]
min_time = 10_000
format = "祥[$duration]($style) "

[time]
time_format = "%I:%M%p"
format = "🕙 $time($style) "
style = "bright-white"
disabled = false

[python]
format = '[${symbol}${pyenv_prefix}${version} (\($virtualenv\))]($style) '

[package]
disabled = true

[git_branch]
format = "[$symbol$branch]($style) "

[git_status]
format = '([$all_status$ahead_behind]($style)) '
style = "bright-white"
conflicted = "𥉉 "
ahead = "落×${count} "
behind = " ×${count} "
diverged = "נּ 落 ×${ahead_count}  ×${behind_count} "
modified = " ×${count} "
untracked = "﬒ ×${count} "
stashed = "﬚  " 
staged = "ﱐ ×${count} "
renamed = " ×${count} "
deleted = " ×${count} "

[username]
show_always = true
style_user = "bold cyan"
format = "[$user]($style)@"

[hostname]
ssh_only = false
format = "[$hostname](blue): "

[directory]
fish_style_pwd_dir_length = 1
read_only = " "
